---
:backends:
  - yaml
:hierarchy:
  - defaults
  - "%{clientcert}"
  - "%{environment}"
  - global

:yaml:
  :datadir: "<%= scope.lookupvar("::settings::confdir") %>/environments/%{environment}/hieradata"
